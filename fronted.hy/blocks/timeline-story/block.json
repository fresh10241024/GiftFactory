{
  "id": "timeline-story",
  "status": "approved",
  "slots": {
    "title": { "type": "string", "required": false, "maxLength": 180 },
    "items": {
      "type": "object[]",
      "required": true,
      "min": 1,
      "max": 12,
      "itemShape": {
        "date": { "type": "string", "required": false, "maxLength": 40 },
        "title": { "type": "string", "required": true, "maxLength": 180 },
        "body": { "type": "string", "required": false, "maxLength": 1000 },
        "image": { "type": "image", "required": false }
      }
    },
    "accentColor": { "type": "color(hex)", "required": false }
  }
}
